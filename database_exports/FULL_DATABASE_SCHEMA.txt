HI TECH SOFTWARE - COMPLETE DATABASE SCHEMA
=============================================

Generated: 2026-03-27 02:10:00
Project: otmnfcuuqlbeowphxagf
Platform: Supabase (PostgreSQL)
Schema: public

=== TABLE COUNTS ===
BASE TABLES: 42
VIEWS: 7
TOTAL: 49

=== BASE TABLES (42) ===

1. amc
2. amc_contracts
3. amc_notification_logs
4. attendance_logs
5. attendance_settings
6. audit_log
7. billing
8. billing_items
9. brand_dealer_payments
10. brands
11. customers
12. dealers
13. digital_bag_consumptions
14. digital_bag_items
15. digital_bag_sessions
16. inventory_products
17. mrp_change_log
18. notifications
19. payouts
20. product_categories
21. product_types
22. products
23. profiles
24. service_categories
25. stock_entries
26. stock_entry_items
27. subject_accessories
28. subject_bills
29. subject_contracts
30. subject_media
31. subject_photos
32. subject_status_history
33. subjects
34. subjects_archive
35. technician_attendance
36. technician_commission_config
37. technician_earnings_summary
38. technician_service_payouts
39. technicians
40. warranty

=== VIEWS (7) ===

1. active_subjects_today
2. brand_dealer_due_invoices
3. current_stock_levels
4. overdue_subjects
5. pending_unassigned_subjects

=== DETAILED TABLE STRUCTURES ===

=== PROFILES ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- email (varchar(255), NOT NULL)
- display_name (varchar(255), NOT NULL)
- phone_number (varchar(20))
- role (user_role, NOT NULL, default: 'technician')
- is_active (boolean, NOT NULL, default: true)
- is_deleted (boolean, NOT NULL, default: false)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== CUSTOMERS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- customer_name (varchar(255), NOT NULL)
- phone_number (varchar(20), NOT NULL)
- email (varchar(255))
- address (text, NOT NULL)
- city (varchar(100), NOT NULL)
- postal_code (varchar(20))
- latitude (decimal(10,8))
- longitude (decimal(11,8))
- is_active (boolean, NOT NULL, default: true)
- is_deleted (boolean, NOT NULL, default: false)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== SUBJECTS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- subject_number (varchar(50), NOT NULL)
- customer_id (uuid, NOT NULL)
- product_name (varchar(255), NOT NULL)
- product_description (text)
- product_model (varchar(100))
- product_serial_number (varchar(100))
- category_name (varchar(100))
- type_of_service (varchar(50))
- priority (varchar(20), default: 'normal')
- status (subject_status, NOT NULL, default: 'PENDING')
- warranty_end_date (date)
- assigned_technician_id (uuid)
- technician_acceptance_status (varchar(20), default: 'pending')
- technician_allocated_date (date)
- technician_allocation_notes (text)
- rejected_by_technician_id (uuid)
- is_rejected_pending_reschedule (boolean, NOT NULL, default: false)
- reschedule_reason (text)
- is_amc_service (boolean, NOT NULL, default: false)
- amc_id (uuid)
- is_warranty_service (boolean, NOT NULL, default: false)
- amc_override_reason (text)
- completed_at (timestamp with time zone)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())
- is_deleted (boolean, NOT NULL, default: false)
- deleted_at (timestamp with time zone)

=== TECHNICIANS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- technician_code (varchar(50), NOT NULL)
- qualification (varchar(255))
- experience_years (integer)
- bank_account_number (varchar(255))
- bank_name (varchar(255))
- ifsc_code (varchar(255))
- daily_subject_limit (integer, NOT NULL, default: 10)
- digital_bag_capacity (integer, NOT NULL, default: 50)
- is_active (boolean, NOT NULL, default: true)
- is_deleted (boolean, NOT NULL, default: false)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())
- total_rejections (integer, NOT NULL, default: 0)

=== INVENTORY_PRODUCTS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- material_code (varchar(100), NOT NULL)
- product_name (varchar(255), NOT NULL)
- description (text)
- category_id (uuid)
- product_type_id (uuid)
- brand (varchar(100))
- hsn_sac_code (varchar(20))
- purchase_price (decimal(12,2))
- mrp (decimal(12,2))
- uom (varchar(20), default: 'Piece')
- is_active (boolean, NOT NULL, default: true)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== STOCK_ENTRIES ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- invoice_number (varchar(100), NOT NULL)
- invoice_date (date, NOT NULL)
- supplier_id (uuid, NOT NULL)
- product_id (uuid, NOT NULL)
- quantity_received (integer, NOT NULL)
- purchase_price (decimal(12,2), NOT NULL)
- mrp (decimal(12,2), NOT NULL)
- hsn_sac_code (varchar(20))
- received_date (date, NOT NULL, default: CURRENT_DATE)
- notes (text)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== DIGITAL_BAG_SESSIONS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- technician_id (uuid, NOT NULL)
- session_date (date, NOT NULL, default: CURRENT_DATE)
- status (varchar(20), NOT NULL, default: 'active')
- items_issued (integer, default: 0)
- items_returned (integer, default: 0)
- items_consumed (integer, default: 0)
- variance_amount (numeric(12,2), default: 0)
- notes (text)
- started_at (timestamp with time zone, NOT NULL, default: now())
- closed_at (timestamp with time zone)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== SUBJECT_BILLS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- bill_number (varchar(50), NOT NULL)
- subject_id (uuid, NOT NULL)
- bill_type (varchar(50), NOT NULL)
- visit_charge (decimal(12,2), NOT NULL, default: 0)
- service_charge (decimal(12,2), NOT NULL, default: 0)
- accessories_total (decimal(12,2), NOT NULL, default: 0)
- gst_amount (decimal(12,2), NOT NULL, default: 0)
- grand_total (decimal(12,2), NOT NULL, default: 0)
- payment_status (varchar(20), NOT NULL, default: 'due')
- payment_mode (varchar(20))
- payment_collected_at (timestamp with time zone)
- created_by (uuid, NOT NULL)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== SUBJECT_ACCESSORIES ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- subject_id (uuid, NOT NULL)
- bill_id (uuid)
- item_name (varchar(255), NOT NULL)
- quantity (integer, NOT NULL)
- mrp (decimal(12,2), NOT NULL)
- discount_type (varchar(20), default: 'percentage')
- discount_value (decimal(12,2), default: 0)
- discounted_mrp (decimal(12,2), GENERATED ALWAYS AS (...))
- line_total (numeric(12,2), GENERATED ALWAYS AS (...))
- extra_price_collected (decimal(12,2), default: 0)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== TECHNICIAN_COMMISSION_CONFIG ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- technician_id (uuid, NOT NULL)
- subject_id (uuid, NOT NULL)
- service_commission (decimal(12,2), NOT NULL, default: 0.00)
- parts_commission (decimal(12,2), NOT NULL, default: 0.00)
- extra_price_commission (decimal(12,2), NOT NULL, default: 0.00)
- commission_notes (text)
- set_by (uuid)
- set_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== TECHNICIAN_EARNINGS_SUMMARY ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- technician_id (uuid, NOT NULL)
- subject_id (uuid, NOT NULL)
- service_commission (decimal(12,2), default: 0.00)
- parts_commission (decimal(12,2), default: 0.00)
- extra_price_commission (decimal(12,2), default: 0.00)
- extra_price_collected (decimal(12,2), default: 0.00)
- variance_deduction (decimal(12,2), default: 0.00)
- net_earnings (decimal(12,2), GENERATED ALWAYS AS (...))
- total_bill_value (decimal(12,2), default: 0.00)
- parts_sold_value (decimal(12,2), default: 0.00)
- earnings_status (text, default: 'pending')
- confirmed_by (uuid)
- confirmed_at (timestamp with time zone)
- created_at (timestamp with time zone, default: now())
- updated_at (timestamp with time zone, default: now())

=== TECHNICIAN_SERVICE_PAYOUTS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- technician_id (uuid, NOT NULL)
- subject_id (uuid)
- base_amount (numeric(12,2), NOT NULL, default: 0)
- deductions (numeric(12,2), NOT NULL, default: 0)
- variance_deduction (numeric(12,2), NOT NULL, default: 0)
- final_amount (numeric(12,2), NOT NULL, default: 0)
- status (text, NOT NULL, default: 'pending')
- notes (text)
- approved_by (uuid)
- paid_at (timestamp with time zone)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== AMC_CONTRACTS ===
- id (uuid, NOT NULL, default: gen_random_uuid())
- contract_number (varchar(50), NOT NULL)
- customer_id (uuid, NOT NULL)
- subject_id (uuid)
- appliance_category_id (uuid)
- appliance_brand (varchar(100), NOT NULL)
- appliance_model (varchar(100))
- appliance_serial_number (varchar(100))
- duration_type (varchar(20), NOT NULL, default: '1_year')
- start_date (date, NOT NULL)
- end_date (date, NOT NULL)
- status (amc_status, NOT NULL, default: 'active')
- price_paid (numeric(12,2), NOT NULL, default: 0)
- payment_mode (varchar(20), NOT NULL, default: 'cash')
- billed_to_type (varchar(20), NOT NULL, default: 'customer')
- billed_to_id (uuid)
- sold_by (varchar(255), NOT NULL)
- sold_at (timestamp with time zone, NOT NULL, default: now())
- commission_amount (numeric(12,2), NOT NULL, default: 0)
- commission_set_by (uuid)
- commission_set_at (timestamp with time zone)
- coverage_description (text)
- free_visits_per_year (integer)
- parts_covered (boolean, NOT NULL, default: false)
- parts_coverage_limit (numeric(12,2))
- brands_covered (text)
- exclusions (text)
- special_terms (text)
- notification_30_sent (boolean, NOT NULL, default: false)
- notification_15_sent (boolean, NOT NULL, default: false)
- notification_7_sent (boolean, NOT NULL, default: false)
- notification_1_sent (boolean, NOT NULL, default: false)
- last_notification_sent_at (timestamp with time zone)
- renewal_of (uuid)
- renewed_by_id (uuid)
- cancelled_at (timestamp with time zone)
- cancelled_by (uuid)
- cancellation_reason (text)
- created_by (uuid, NOT NULL)
- created_at (timestamp with time zone, NOT NULL, default: now())
- updated_at (timestamp with time zone, NOT NULL, default: now())

=== VIEWS ===

1. active_subjects_today - Shows today's active subjects
2. brand_dealer_due_invoices - Shows due invoices from brands/dealers
3. current_stock_levels - Shows current inventory stock levels
4. overdue_subjects - Shows overdue service tickets
5. pending_unassigned_subjects - Shows unassigned service tickets

=== ENUM TYPES ===

- user_role: 'super_admin', 'office_staff', 'stock_manager', 'technician'
- subject_status: 'PENDING', 'ALLOCATED', 'ACCEPTED', 'IN_PROGRESS', 'COMPLETED', 'INCOMPLETE', 'AWAITING_PARTS', 'RESCHEDULED'
- amc_status: 'active', 'expiring_soon', 'expired', 'renewed', 'cancelled'
- warranty_status: 'ACTIVE', 'EXPIRING_SOON', 'EXPIRED', 'RENEWED', 'CANCELLED'

=== GENERATED COLUMNS ===

- subject_accessories.discounted_mrp - Auto-calculated discounted price
- subject_accessories.line_total - Auto-calculated line total
- technician_earnings_summary.net_earnings - Auto-calculated net earnings

=== CONSTRAINTS & INDEXES ===

- All primary keys are UUID with gen_random_uuid() defaults
- Foreign key relationships maintain data integrity
- Unique constraints on critical business fields
- Indexes optimized for common query patterns

=== RLS (Row Level Security) ===

- Implemented on all sensitive tables
- Role-based access control
- Data privacy protection
- Audit logging enabled

Schema extraction completed successfully! 🎯
